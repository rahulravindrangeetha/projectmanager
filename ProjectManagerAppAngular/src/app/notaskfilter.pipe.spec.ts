import { NoTaskfilterPipe } from './notaskfilter.pipe';

describe('NoTaskfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NoTaskfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
